class Product {
  final String id, title, description, image, status;
  final int price;
  final int quantity;

  Product(this.title, this.description, this.price, this.image, this.id,
      this.quantity, this.status);
}
